[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "doctransmittal"
version = "1.3.0"
description = "Document Transmittal System"
authors = [{ name="William Arnott"}]

[tool.setuptools.packages.find]
include = ["doctransmittal_sub*"]
